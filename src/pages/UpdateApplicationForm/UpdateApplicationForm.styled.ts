import styled from '@emotion/styled';

export const UpdateApplicationFormPage = styled.div`
  padding: 2rem 0 4rem;

  & > form {
    display: flex;

    & > article {
      flex: 1;
    }

    & > aside {
      margin-left: 1.6rem;
    }
  }
`;
